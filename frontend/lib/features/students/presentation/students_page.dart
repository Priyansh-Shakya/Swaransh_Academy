import 'package:flutter/material.dart';

/// Placeholder. Real content (filterable list, StudentBasic/StudentFull
/// role-aware rendering) comes in the Students feature chunk.
class StudentsPage extends StatelessWidget {
  const StudentsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Students')),
      body: const Center(child: Text('Students list - coming soon')),
    );
  }
}
