from users import model

async def create_user(user: model.User_Create) -> model.User_Read:
    # DB query impl.
    return 


async def get_user_by_id(id: int) -> model.User_Read:
    # DB impl
    return 
